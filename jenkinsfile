pipeline {
   agent {
  
        // Run on a build agent where we have the Android and iOS are configured
        label 'reactNative'
  }
     environment {
            CI = 'true'
        }
        stage('yarn install') {
            steps {
                sh 'yarn install'
            }
        }
}